# 多平台博主监控系统 - 快速开始指南

## 🎯 系统功能

每日自动检查指定博主是否发布新内容，如果有：
1. ✅ 检测新内容（通过ID判断，不重复）
2. ✅ 生成AI摘要
3. ✅ 生成详细日报（Markdown格式）
4. ✅ 生成简短摘要（TXT格式）

## 🚀 快速开始

### 步骤1：添加博主

**方式A：编辑配置文件**
```bash
cd /home/admin/openclaw/workspace/projects/social-content-creator
nano data/multi_platform_bloggers.json
```

添加博主：
```json
{
  "bloggers": [
    {
      "platform": "bilibili",
      "name": "博主名称",
      "mid": "博主ID",
      "enabled": true
    }
  ],
  "last_check": null
}
```

**方式B：使用Web界面（推荐）**
```bash
python3 app_multi.py
# 访问 http://localhost:5000
```

### 步骤2：运行监控

**方式A：手动运行**
```bash
python3 multi_platform_monitor.py
```

**方式B：设置定时任务**
```bash
# 编辑crontab
crontab -e

# 每天早上8:30执行
30 8 * * * cd /home/admin/openclaw/workspace/projects/social-content-creator && python3 multi_platform_monitor.py >> logs/cron.log 2>&1
```

### 步骤3：查看日报

**详细日报**：`data/multi_platform_reports/YYYY-MM-DD.md`
**简短摘要**：`data/multi_platform_reports/YYYY-MM-DD_summary.txt`

示例：
```markdown
# 多平台博主日报

**日期**: 2026-02-13
**生成时间**: 10:00:00

---

## 📊 今日统计

- **新内容数量**: 3
- **涉及平台**: 1
- **B站**: 3 条

## 📺 B站

### 视频标题

**博主**: 老番茄
**热度**: 热门 | **播放**: 100,000 | **评论**: 5,000

**AI摘要**: 视频描述...
**话题标签**: AI, 教程
```

## 📋 博主ID获取

### B站
1. 访问博主主页：`https://space.bilibili.com/{mid}`
2. URL中的数字就是mid
3. 例如：`https://space.bilibili.com/546195` → mid = `546195`

### 小红书
1. 运行本地脚本：`python3 xiaohongshu_local_crawler.py {user_id}`
2. 将生成的 `xiaohongshu_user_{user_id}_data.json` 上传到 `data/` 目录
3. 在配置文件中添加：
```json
{
  "platform": "xiaohongshu",
  "name": "博主名称",
  "user_id": "user_id",
  "enabled": true
}
```

## 🔧 配置说明

### 修改检查频率
编辑 `multi_platform_monitor.py`，找到：
```python
# B站需要等待避免频率限制
if blogger['platform'] == 'bilibili' and i < len(bloggers):
    await asyncio.sleep(60)  # 改为30或90秒
```

### 修改获取数量
```python
# get_bilibili_videos(user_id, num=20)  # 改为10或30
```

## 📁 文件结构

```
data/
├── multi_platform_bloggers.json     # 博主列表
├── multi_platform_content.json      # 内容记录
└── multi_platform_reports/         # 日报目录
    ├── 2026-02-13.md              # 详细日报
    └── 2026-02-13_summary.txt     # 简短摘要
```

## ⚠️ 常见问题

### Q1: 频率限制怎么办？
**A**: 增加请求间隔到90-120秒，或减少博主数量

### Q2: 检测不到新内容？
**A**:
1. 检查 `multi_platform_content.json` 是否已记录
2. 如果是新博主，第一条会被记录为"已看"
3. 只有**第一次运行之后**发布的内容才会被报告

### Q3: 小红书怎么监控？
**A**:
1. 在本地运行 `xiaohongshu_local_crawler.py`
2. 上传生成的JSON到服务器 `data/` 目录
3. 重命名为 `xiaohongshu_user_data.json`
4. 运行监控系统

### Q4: 如何删除博主？
**A**: 编辑 `multi_platform_bloggers.json`，设置 `"enabled": false`

## 💡 最佳实践

### 1. 分批监控
- 如果博主超过20个，分多批监控
- 每批间隔30分钟

### 2. 错峰执行
- 推荐时间：早上6-8点
- 避免时间：晚上8-11点

### 3. 定期检查
- 每天监控1次即可
- 设置定时任务自动执行

## 🎁 扩展功能

### 添加通知功能（待开发）
- 邮件通知
- 钉钉/企业微信
- Telegram推送

### AI摘要增强（待开发）
- 接入真实AI API
- 内容深度分析
- 智能推荐

---

**开发者**: 智宝 (AI助手) 🌸
**版本**: v2.1 集成版
**最后更新**: 2026-02-13
