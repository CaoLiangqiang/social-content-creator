# Social Content Creator Platform (SCCP)

社交内容创作平台 - 基于AI的多平台内容采集、分析与生成系统

## 🎯 项目概述

构建一个基于AI的社交内容创作平台，能够自动获取、分析和生成符合各大社交平台的爆款内容。

### 核心价值
- 🔥 提升内容创作效率
- 🎯 精准把握平台调性
- 📊 数据驱动的内容优化
- 🚀 自动化内容生产流程

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    用户界面层 (UI Layer)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Web App    │  │  Mobile Web  │  │   CLI Tool   │      │
│  │   (React)    │  │  (PWA)       │  │   (Python)   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────────┐
│                       API 网关层 (Gateway)                   │
│     认证 / 限流 / 路由 / 日志 / 监控 / 负载均衡              │
└─────────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────────┐
│                      业务服务层 (Services)                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 用户服务  │ │ 爬虫服务  │ │ 分析服务  │ │ 发布服务  │       │
│  │ (Node)   │ │ (Python) │ │ (Node)   │ │ (Node)   │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ AI服务   │ │ 监控服务  │ │ 通知服务  │ │ 存储服务  │       │
│  │ (Python) │ │ (Node)   │ │ (Node)   │ │ (Node)   │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────────┐
│                      数据存储层 (Storage)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │PostgreSQL│ │ MongoDB  │ │  Redis   │ │    ES    │       │
│  │ (主数据) │ │ (原始数据)│ │ (缓存/队列)│ │ (搜索)   │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
```

## 🛠️ 技术栈

### 前端
- **框架**: React 18 + Vite
- **UI库**: Ant Design
- **状态管理**: Zustand
- **HTTP客户端**: Axios

### 后端
- **框架**: Node.js + Express
- **数据库**: PostgreSQL 15, MongoDB 6, Redis 7
- **认证**: JWT
- **日志**: Winston

### 爬虫
- **框架**: Python + aiohttp + BeautifulSoup
- **浏览器自动化**: Playwright
- **任务队列**: Redis Queue

### AI服务
- **框架**: Python + FastAPI
- **AI模型**: OpenAI GPT-4 / Claude
- **备选**: 本地模型支持

## 📁 项目结构

```
social-content-creator/
├── .github/workflows/     # CI/CD配置
├── db/
│   └── init.sql          # 数据库初始化脚本
├── docs/                 # 文档
│   ├── PRODUCT_DESIGN.md # 产品设计文档
│   ├── ARCHITECTURE.md   # 架构设计文档
│   ├── DATABASE_SCHEMA.md# 数据库设计文档
│   └── DEPLOYMENT.md     # 部署指南
├── src/
│   ├── ai/               # AI服务 (Python/FastAPI)
│   ├── backend/          # 后端服务 (Node.js/Express)
│   ├── crawler/          # 爬虫服务 (Python)
│   ├── frontend/         # 前端应用 (React/Vite)
│   ├── storage/          # 存储服务
│   └── utils/            # 工具脚本
├── docker-compose.yml    # Docker编排配置
├── package.json          # 根项目配置
└── README.md             # 本文件
```

## 🚀 快速开始

### 环境要求
- Node.js >= 20.0.0
- Python >= 3.10
- Docker & Docker Compose
- PostgreSQL 15
- Redis 7
- MongoDB 6

### 1. 克隆项目

```bash
git clone <repository-url>
cd social-content-creator
```

### 2. 配置环境变量

```bash
# 复制环境变量模板
cp src/backend/.env.example src/backend/.env
cp .env.example .env

# 编辑 .env 文件，配置数据库连接和API密钥
```

### 3. 启动数据库服务

```bash
docker-compose up -d postgres redis mongodb
```

### 4. 安装依赖

```bash
# 安装后端依赖
cd src/backend
npm install

# 安装前端依赖
cd ../frontend
npm install

# 安装Python依赖
cd ../crawler
pip install -r requirements.txt

cd ../ai
pip install -r requirements.txt
```

### 5. 初始化数据库

```bash
# 执行数据库初始化脚本
psql -U postgres -d sccp -f db/init.sql
```

### 6. 启动服务

```bash
# 方式1: 使用Docker Compose（推荐）
docker-compose up -d

# 方式2: 手动启动各服务
# 终端1: 启动后端
cd src/backend
npm run dev

# 终端2: 启动前端
cd src/frontend
npm run dev

# 终端3: 启动AI服务
cd src/ai
python main.py
```

### 7. 访问应用

- 前端界面: http://localhost:5173
- 后端API: http://localhost:3000/api/v1
- 健康检查: http://localhost:3000/api/v1/health

## 📚 核心功能

### 1. 多平台爬虫
- **小红书**: 图文笔记、用户评论、话题标签
- **B站**: 视频信息、弹幕、评论、UP主动态
- **抖音**: 视频内容、用户数据

### 2. 内容分析
- 情感分析
- 关键词提取
- 爆款潜力评分
- 趋势预测

### 3. AI内容生成
- 小红书风格内容生成
- 标题优化
- 标签推荐
- 多平台适配

### 4. 发布管理
- 定时发布
- 多平台同步
- 效果追踪

### 5. 博主监控
- 多平台博主追踪
- 智能日报生成
- 新内容提醒

## 🔧 配置说明

### 后端配置 (.env)

```env
# Server Configuration
NODE_ENV=development
PORT=3000
API_PREFIX=/api/v1

# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=sccp
POSTGRES_USER=sccp_user
POSTGRES_PASSWORD=sccp_password

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/sccp

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d

# AI Service Configuration
AI_SERVICE_URL=http://localhost:8000
OPENAI_API_KEY=your-openai-api-key
```

## 🧪 测试

```bash
# 后端测试
cd src/backend
npm test

# 爬虫测试
cd src/crawler
python -m pytest tests/

# 前端测试
cd src/frontend
npm test
```

## 📝 API文档

API文档可通过以下方式查看：
- Swagger UI: http://localhost:3000/api-docs (开发环境)
- Postman Collection: 导入 `docs/postman_collection.json`

## 🐳 Docker部署

### 生产环境部署

```bash
# 构建所有镜像
docker-compose -f docker-compose.yml build

# 启动生产环境
docker-compose -f docker-compose.yml up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

### 环境变量

生产环境需要配置以下环境变量：
- `JWT_SECRET`: JWT签名密钥（至少32位）
- `OPENAI_API_KEY`: OpenAI API密钥
- `DB_PASSWORD`: 数据库密码
- `REDIS_PASSWORD`: Redis密码（可选）

## 📖 详细文档

- [产品设计文档](docs/PRODUCT_DESIGN.md) - 产品功能规划
- [架构设计文档](docs/ARCHITECTURE.md) - 系统架构设计
- [数据库设计](docs/DATABASE_SCHEMA.md) - 数据库Schema设计
- [部署指南](docs/DEPLOYMENT.md) - 详细部署说明

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 📄 许可证

[MIT](LICENSE)

## 🙏 致谢

- OpenAI - AI能力支持
- Ant Design - UI组件库
- Express.js - 后端框架
- React - 前端框架

---

**版本**: v1.0.0  
**维护者**: Development Team  
**最后更新**: 2026-02-13
